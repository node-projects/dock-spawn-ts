{
  "name": "dockspawn-ts-ui",
  "description": "DockSpawn",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc"
  },
  "devDependencies": {
    "typescript": "^3.3.4000",
    "yarn": "^1.16.0",
    "@types/node": "^12.0.4"
  }
}
